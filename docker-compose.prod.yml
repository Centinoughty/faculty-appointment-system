services:
  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: fams-client
    command: npm start
    env_file:
      - ./client/.env
    ports:
      - "3000:3000"
    networks:
      - app-network

networks:
  app-network:
    name: fams-net
    driver: bridge
